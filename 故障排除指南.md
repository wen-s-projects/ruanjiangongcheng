# 卡卡项目 - 故障排除指南

## 🔍 问题诊断

### 问题：后端无法启动

#### 可能原因1：Django未安装
**症状**：运行`python manage.py runserver`时提示"找不到模块"

**解决方案**：
1. 打开CMD（不是PowerShell）
2. 运行：`python -m pip install Django`
3. 验证安装：`python -c "import django; print(django.get_version())"`

#### 可能原因2：依赖缺失
**症状**：启动时提示缺少模块

**解决方案**：
```bash
python -m pip install Django djangorestframework django-cors-headers python-decouple pymysql bcrypt Pillow boto3 markdown bleach PyJWT
```

#### 可能原因3：数据库连接失败
**症状**：提示数据库连接错误

**解决方案**：
1. 检查MySQL是否运行
2. 检查`.env`文件中的数据库配置：
   ```
   DB_NAME=CalorieSystem
   DB_USER=root
   DB_PASSWORD=mysql
   DB_HOST=localhost
   DB_PORT=3306
   ```

#### 可能原因4：端口被占用
**症状**：提示端口8000已被使用

**解决方案**：
1. 检查端口占用：`netstat -ano | findstr :8000`
2. 杀死占用进程：`taskkill /PID <进程ID> /F`
3. 或使用其他端口：`python manage.py runserver 0.0.0.0:8001`

---

## 🚀 推荐启动方法

### 方法1：使用完整安装脚本（推荐）

**步骤**：
1. 双击运行：`安装并启动后端.bat`
2. 脚本会自动：
   - 检查Python
   - 安装Django和依赖
   - 执行数据库迁移
   - 启动服务器

### 方法2：手动启动（如果脚本不工作）

**步骤**：
1. 打开CMD（不是PowerShell）
2. 依次执行：
   ```bash
   cd C:\Users\wxxoxxw\ruanjiangongcheng\backend\calorie_backend
   python -m pip install Django djangorestframework django-cors-headers python-decouple pymysql bcrypt Pillow boto3 markdown bleach PyJWT
   python manage.py migrate
   python manage.py runserver 0.0.0.0:8000
   ```

---

## ✅ 验证后端是否启动成功

### 方法1：访问根URL
打开浏览器访问：http://localhost:8000

**成功标志**：看到Django欢迎页面或API响应

### 方法2：运行测试脚本
```bash
python test_backend.py
```

**成功标志**：看到API响应状态码200

### 方法3：检查进程
```bash
netstat -ano | findstr :8000
```

**成功标志**：看到LISTENING状态的TCP连接

---

## 📝 测试注册功能

### 步骤1：确保后端运行
访问：http://localhost:8000

### 步骤2：访问前端
访问：http://localhost:5175

### 步骤3：注册新用户
1. 点击"还没有账号？立即注册"
2. 填写表单：
   - 用户名：`testuser`
   - 密码：`testpass123`
   - 确认密码：`testpass123`
3. 点击"注册"按钮

**成功标志**：
- 显示"注册成功"提示
- 自动跳转到登录页面
- 可以使用新账号登录

---

## 🔧 常见错误及解决方案

### 错误1：ModuleNotFoundError: No module named 'django'
**原因**：Django未安装

**解决方案**：
```bash
python -m pip install Django
```

### 错误2：django.db.utils.OperationalError: no such table
**原因**：数据库未迁移

**解决方案**：
```bash
python manage.py migrate
```

### 错误3：pymysql.err.OperationalError: Access denied
**原因**：数据库密码错误

**解决方案**：
1. 检查`.env`文件中的`DB_PASSWORD`
2. 确保MySQL用户密码正确

### 错误4：ConnectionRefusedError: [WinError 10061]
**原因**：后端未启动

**解决方案**：
1. 启动Django服务器
2. 确保看到"Starting development server"消息

---

## 📞 获取帮助

如果以上方法都无法解决问题，请：

1. 检查Python版本：`python --version`（需要3.8+）
2. 检查pip版本：`python -m pip --version`
3. 查看详细错误日志
4. 尝试重新安装Python

---

## ✅ 成功标志

后端启动成功后，您应该看到：

```
Watching for file changes with StatReloader
Performing system checks...

System check identified no issues (0 silenced).

January 06, 2026 - 10:00:00
Django version 4.2.27, using settings 'calorie_backend.settings'
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

---

## 🎯 下一步

后端启动成功后：

1. ✅ 访问前端：http://localhost:5175
2. ✅ 测试注册功能
3. ✅ 登录系统
4. ✅ 使用所有功能
